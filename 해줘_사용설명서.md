# ✍️ 해줘 — SillyTavern 대필 확장 사용설명서 (v1.11.2)

## 이게 뭐 하는 확장이야?

**해줘**는 SillyTavern에서 유저(페르소나)가 보낼 다음 메시지를 AI가 대신 써주는 대필 확장이야.
장르·톤·인칭·시제·감정 같은 조건을 골라주면, AI가 지금 대화 흐름에 맞는 답장을 만들어줘. 마음에 안 들면 재생성하거나 지시사항 추가해서 다시 뽑을 수 있고, 결과를 그대로 채팅 입력창에 넣어서 보내면 끝.

캐릭터가 아니라 **유저 쪽 메시지**를 써주는 확장이라는 게 핵심이야.

---

## 어떻게 작동해?

1. 채팅 입력창 옆 완드 버튼(✍️)을 누르면 **대필 설정 팝업**이 뜸
2. 팝업에서 프리셋, 장르, 작성 방식, 톤, 인칭, 시제, 언어, 길이, 감정 등을 고름
3. "대필 생성" 누르면 AI가 생성 시작 (로딩 중 **취소** 버튼으로 중단 가능)
4. 결과 팝업이 뜨고, 거기서 **삽입 / 재생성 / 수정 재생성 / 번역 / 히스토리 이동**을 선택
5. "인풋에 삽입" 누르면 채팅 입력창에 텍스트가 들어가고, 유저가 확인 후 직접 전송

**진입점**: 완드 버튼(팝업 열기)이 메인이고, 확장 탭엔 **연결 프로필 지정용** 패널만 따로 있음.

**컨텍스트 처리 원리**: 채팅 히스토리를 통째로 다시 넣지 않아. ST가 `generateQuietPrompt` 호출할 때 캐릭터카드·월드인포·대화 히스토리를 자동으로 컨텍스트에 포함시키기 때문에, 확장은 그 위에 지시사항만 얹어서 보내. 대신 **직전 상황을 확실히 놓치지 않도록, 최근 3턴을 프롬프트 끝부분에 직접 인용**해서 "이 흐름에 반드시 이어서 써라"라고 못 박아둠 (모델이 엉뚱한 상황을 지어내는 걸 방지).

**형식 오염 방지**: 캐릭터 메시지에 날씨·날짜·상태창 같은 정보블록이 섞여 있어도, 유저 본인이 실제로 썼던 최근 메시지 스타일을 참고해서 그런 정보블록/시스템 태그를 절대 따라 하지 않고 순수한 유저 말투로만 나오게 함.

---

## 설정은 어떻게 저장돼?

**팝업에서 뭘 누르든 그 즉시 저장돼.** 장르, 작성방식, 인칭, 시제, 언어, 길이, 톤 슬라이더, 토큰 수, 3인칭 이름 — 전부 클릭하거나 값을 바꾸는 순간 바로 저장되고, "대필 생성"을 안 누르고 그냥 팝업을 닫아버려도 그 상태가 남아있음. ST를 껐다 켜도 마지막으로 만졌던 설정 그대로 유지됨.

---

## 프리셋

자주 쓰는 설정 조합(장르·톤·인칭·시제·길이 등 전부)을 저장해두고 불러올 수 있음.

- **드롭다운**: 프리셋 선택하면 그 설정으로 팝업이 즉시 바뀜. 마지막으로 썼던 프리셋도 기억해서 다음에 팝업 열 때 그대로 표시됨
- **💾 저장**: 선택 중인 프리셋에 지금 설정을 그대로 덮어씀 (이름은 안 바뀜). 저장하면 "✅ 저장 완료!" 문구가 잠깐 떴다 사라짐
- **➕ 새로 만들기**: 이름을 새로 입력받아서 지금 설정을 새 프리셋으로 추가
- **🗑️ 삭제**: 선택한 프리셋 삭제 ("🗑️ 삭제됨" 메시지 표시)

---

## 연결 프로필 지정 (확장 탭)

확장 탭 "해줘" 패널에서 **대필 생성 전용 연결 프로필**을 따로 지정할 수 있음.

- ST에 저장해둔 커넥션 프로필 목록이 드롭다운에 표시됨 (🔄 버튼으로 새로고침)
- 프로필을 선택하면, 대필 생성할 때만 **그 프로필로 잠깐 전환했다가 끝나면 원래 프로필로 자동 복구**함
- 화면 깜빡임이나 별도 조작 없이 자동으로 처리됨 (예: 평소엔 비싼 모델 쓰다가, 대필만 저렴한 모델로 돌리고 싶을 때 유용)
- "현재 활성 프로필 사용"으로 두면 그냥 지금 ST에 연결된 API 그대로 사용

---

## 설정 팝업에서 고를 수 있는 것들

### 장르
- 기본 5개: 로맨스코메디, 코메디, 일상, 공포, 슬픔
- "장르 관리" 버튼으로 자유롭게 추가·수정·삭제 (팝업이 사라지지 않고 잠깐 숨겨졌다가 다시 나타남)
- 한글/영어 다 인식 (`dark romance`처럼 영어도 가능)

### 작성 방식
서술+대사 / 대사만 / 서술만

### 톤 조절 (슬라이더 2개)
- **격식체 ↔ 반말/구어체**, **진지함 ↔ 가벼움/장난기**
- **장르 선택하면 자동으로 어울리는 톤으로 슬라이더 이동** (예: 공포→격식+진지, 코메디→반말+장난기 최대)
- 장르 이름에 "다크/dark"가 들어가면 장난기 수치 자동으로 30 낮춤 (`dark romance`, `다크코메디`)
- 자동 추천된 값도 손으로 다시 조절 가능 (슬라이더에서 손 뗄 때 자동 저장)

### 인칭
1인칭(나/저) / 3인칭(그/그녀). 3인칭이면 이름 입력해서 그 이름으로 서술 가능 (비워두면 그/그녀)

### 시제
과거 / 현재 / 미래 중 선택. 메시지 전체 시제를 통일해줌

### 출력 언어
한국어 / English

### 길이
✂️ 짧게(3문장 이내) / 📝 보통(10문장 이내) / 📖 길게(제한 없음)

### 감정 태그
14개 감정 칩(기쁨·설렘·슬픔·분노·당황·부끄러움·질투·긴장·놀람·그리움·안도·짜증·애교·걱정) 클릭하면 지시사항 칸에 자동 추가. 여러 개 조합 가능.

### 지시사항 자동 번역 (토글)
켜두면 한국어로 쓴 지시사항을 내부적으로 영어로 번역해서 AI에게 전달

### 지시사항 입력창
자유 텍스트로 세부 지시. 비워두면 AI가 대화 흐름 보고 알아서 판단

### 최대 응답 토큰 / 번역 최대 토큰
대필 결과와 번역 결과 각각 토큰 제한. 0 = 무제한. (주의: 이건 *출력* 길이 제한이지 컨텍스트 크기가 아니라서, 크게 잡는다고 맥락을 더 잘 읽는 건 아님 — 실제 답장엔 500~1000 정도면 충분함)

---

## 생성 중 취소

로딩 화면에 **취소** 버튼이 있음. 누르면 그 생성은 무시되고 로딩창이 바로 닫힘. 지시사항 자동 번역이 켜져 있으면 "지시사항 번역 중…" → "AI 대필 중…"으로 단계별 문구도 정확히 표시됨.

---

## 결과 팝업에서 할 수 있는 것들

- **인풋에 삽입**: 결과를 채팅 입력창에 넣음 (직접 수정한 뒤 삽입 가능)
- **재생성**: 같은 설정으로 다시 생성
- **수정 재생성**: "더 짧게", "더 슬프게" 같은 추가 지시를 넣고 이전 결과를 참고해서 재생성
- **번역**: 결과를 반대 언어로 번역해서 아래에 표시. 번역문도 직접 수정 가능하고, "번역본 삽입" 버튼으로 번역된 텍스트만 따로 입력창에 넣을 수 있음
- **히스토리 이동**: 같은 세션에서 여러 번 생성했다면 ◀이전 / 다음▶ 으로 오갈 수 있음 (새로고침하면 초기화)

결과 상단 태그에는 장르·작성방식·톤·언어·시제·글자수가 표시됨.

---

## 인풋에 삽입한 뒤

채팅 입력창 바로 아래에 작은 **인포바**가 떠서 지금 적용된 장르·작성방식·지시사항 요약을 계속 보여줌. ✕ 누르면 닫힘.

---

## 디자인

- ST에서 쓰고 있는 테마 색상을 그대로 따라감
- 밝은 테마면 글씨 어둡게, 어두운 테마면 글씨 밝게 자동 전환
- 팝업은 항상 화면 정중앙에 고정 (모바일 포함)
- 토글 스위치는 네이티브 체크박스를 안 쓰고 순수 버튼으로 구현해서 ST 전역 스타일과 안 겹침

---

## 알아두면 좋은 점

- 캐릭터카드, 월드인포, 대화 히스토리는 ST가 알아서 컨텍스트에 넣어주니까 확장이 따로 안 챙겨도 됨
- 페르소나 디스크립션은 자동으로 읽어서 프롬프트에 반영됨
- 세션 히스토리(◀▶ 이동용)는 새로고침하면 초기화됨 — 계속 남기고 싶은 결과는 미리 복사해두는 게 안전
- 프리셋, 마지막으로 만졌던 설정, 연결 프로필 지정은 전부 영구 저장됨 (ST 껐다 켜도 유지)
- 문제 생기면 브라우저 개발자도구(F12) 콘솔에서 `[해줘]`로 시작하는 로그로 원인 확인 가능
