# ✍️ 한마디 — SillyTavern 대필 확장 사용설명서 (v2.0.0)

> 이전 이름 "해줘"에서 **한마디**로 변경되었습니다. 설정·프리셋은 자동으로 그대로 이어집니다.

## 이게 뭐 하는 확장이야?

**한마디**는 SillyTavern에서 유저(페르소나)가 보낼 다음 메시지를 AI가 대신 써주는 대필 확장이야.
장르·톤·인칭·시제·감정 같은 조건을 골라주면, AI가 지금 대화 흐름에 맞는 답장을 만들어줘. 마음에 안 들면 재생성하거나 지시사항 추가해서 다시 뽑을 수 있고, 결과를 그대로 채팅 입력창에 넣어서 보내면 끝.

캐릭터가 아니라 **유저 쪽 메시지**를 써주는 확장이라는 게 핵심이야.

---

## 어떻게 작동해?

1. 채팅 입력창 옆 완드 버튼(✍️)을 누르면 **대필 설정 팝업**이 뜸
2. 팝업에서 프리셋, 장르, 작성 방식, 톤, 인칭, 시제, 언어, 길이, 감정, 생성 개수 등을 고름
3. "대필 생성" 누르면 AI가 생성 시작 (로딩 중 **취소** 버튼으로 중단 가능)
4. 결과 팝업이 뜨고, 거기서 **삽입 / 재생성 / 수정 재생성 / 번역 / 히스토리 이동**을 선택
5. "인풋에 삽입" 누르면 채팅 입력창에 텍스트가 들어가고, 유저가 확인 후 직접 전송

**진입점**: 완드 버튼(팝업 열기)이 메인이고, 확장 탭엔 **연결 프로필 지정용** 패널만 따로 있음.

**컨텍스트 처리 원리**: 채팅 히스토리를 통째로 다시 넣지 않아. ST가 `generateQuietPrompt` 호출할 때 캐릭터카드·월드인포·대화 히스토리를 자동으로 컨텍스트에 포함시키기 때문에, 확장은 그 위에 지시사항만 얹어서 보내. 대신 **직전 상황을 확실히 놓치지 않도록, 최근 3턴을 프롬프트 끝부분에 직접 인용**해서 "이 흐름에 반드시 이어서 써라"라고 못 박아둠.

**형식 오염 방지**: 캐릭터 메시지에 날씨·날짜·상태창 같은 정보블록이 섞여 있어도, 유저 본인이 실제로 썼던 최근 메시지 스타일을 참고해서 그런 정보블록/시스템 태그를 절대 따라 하지 않고 순수한 유저 말투로만 나오게 함.

---

## 설정은 즉시 저장됨

팝업에서 뭘 누르든 그 즉시 저장돼. 장르, 작성방식, 인칭, 시제, 언어, 길이, 톤 슬라이더, 토큰 수, 생성 개수, 3인칭 이름 — 전부 클릭하거나 값을 바꾸는 순간 바로 저장되고, "대필 생성"을 안 누르고 그냥 팝업을 닫아버려도 그 상태가 남아있음. ST를 껐다 켜도 마지막으로 만졌던 설정 그대로 유지됨.

---

## 프리셋

자주 쓰는 설정 조합을 저장해두고 불러올 수 있음.

- **드롭다운**: 프리셋 선택하면 그 설정으로 팝업이 즉시 바뀜. 마지막으로 썼던 프리셋도 기억해서 다음에 팝업 열 때 그대로 표시됨
- **💾 저장**: 선택 중인 프리셋에 지금 설정을 그대로 덮어씀 (이름은 안 바뀜)
- **➕ 새로 만들기**: 이름을 새로 입력받아서 지금 설정을 새 프리셋으로 추가
- **🗑️ 삭제**: 선택한 프리셋 삭제

---

## 연결 프로필 지정 (확장 탭)

확장 탭 "한마디" 패널에서 **대필 생성 전용 연결 프로필**을 따로 지정할 수 있음.

- ST에 저장해둔 커넥션 프로필 목록이 드롭다운에 표시됨 (🔄 버튼으로 새로고침)
- 프로필을 선택하면, 대필 생성할 때만 **그 프로필로 잠깐 전환했다가 끝나면 원래 프로필로 자동 복구**함 (화면 깜빡임 없이 자동 처리)
- "현재 활성 프로필 사용"으로 두면 그냥 지금 ST에 연결된 API 그대로 사용

---

## 설정 팝업에서 고를 수 있는 것들

### 장르
기본 5개(로맨스코메디·코메디·일상·공포·슬픔) + "장르 관리"로 자유 추가·수정·삭제. 한글/영어 다 인식.
톤 슬라이더 자동 추천은 **AI 판단이 아니라 미리 정해둔 키워드 매칭**이라, 목록에 없는 완전히 새로운 장르명은 중간값(50/50)으로 시작함.

### 작성 방식
서술+대사 / 대사만 / 서술만

### 톤 조절 (슬라이더 2개)
- **격식체 ↔ 반말/구어체**, **진지함 ↔ 가벼움/장난기**
- 장르 선택하면 자동으로 어울리는 톤으로 슬라이더 이동 (장르명에 "다크/dark" 들어가면 장난기 자동으로 30 낮춤)
- 손으로 직접 조절도 가능 (슬라이더에서 손 뗄 때 자동 저장)

### 인칭
1인칭(나/저) / 3인칭(그/그녀, 이름 지정 가능)

### 시제
과거 / 현재 / 미래

### 출력 언어
한국어 / English

### 길이 — 4가지
- ✂️ 짧게(3문장 이내) / 📝 보통(10문장 이내) / 📖 길게(제한 없음)
- 🎯 **직접 지정**: 숫자로 정확한 글자수를 지정 (예: "정확히 50자 이내로"). 선택하면 입력창이 나타남

### 생성 개수
- **1개**: 평소처럼 한 번 생성
- **3개**: 같은 설정으로 3개를 동시에(병렬) 생성해서 결과 중 고를 수 있음
  - 각 버전 카드에서 바로 **삽입**하거나, **🔄 이 버전만 재생성**, 또는 **⤢ 자세히**(재생성·수정재생성·번역 다 가능한 상세 화면으로 확장) 선택 가능
  - 상세 화면에서 X 누르면 다시 3개 목록으로 돌아감 (완전히 닫히지 않음)
  - 마음에 드는 게 없으면 "N개 다시 생성"으로 통째로 재시도
  - ⚠️ 3개 동시 생성이라 토큰/비용은 단순히 3배 듦

### 감정 태그
14개 감정 칩 클릭하면 지시사항 칸에 자동 추가 (복수 선택 가능)

### 지시사항 자동 번역 (토글)
켜두면 한국어 지시사항을 내부적으로 영어로 번역해서 AI에게 전달

### 지시사항 입력창 / 최대 응답 토큰 / 번역 최대 토큰
자유 텍스트 지시 + 각각 토큰 제한 (0 = 무제한). *주의: 이건 출력 길이 제한이지 컨텍스트 크기가 아니라서, 크게 잡는다고 맥락을 더 잘 읽는 건 아님*

---

## 생성 중 취소

로딩 화면에 **취소** 버튼. 누르면 그 생성 무시하고 로딩창 바로 닫힘. 3개 동시 생성 중에도 취소 가능.

---

## 결과 팝업에서 할 수 있는 것들

- **인풋에 삽입** (직접 수정 후 삽입 가능) / **재생성** / **수정 재생성** (추가 지시 반영)
- **번역**: 반대 언어로 번역해서 표시, 번역문 수정 가능, "번역본 삽입"으로 번역본만 따로 넣기
- **히스토리 이동**: ◀이전/다음▶ (새로고침하면 초기화)

결과 상단 태그에는 장르·작성방식·톤·언어·시제·글자수 표시. **장르는 슬라이더 계산용으로만 쓰이는 게 아니라, AI에게 보내는 지시문 텍스트 안에도 그대로 들어가서 두 번 강조됨.**

---

## 인풋에 삽입한 뒤

입력창 아래 작은 **인포바**가 현재 설정 요약을 보여줌. ✕로 닫기 가능.

---

## 디자인

ST 테마 색상 자동 반영, 밝은/어두운 테마에 맞춰 글씨색 자동 전환, 팝업은 항상 화면 정중앙 고정(모바일 포함), 토글 스위치는 ST 전역 스타일과 안 겹치게 순수 버튼으로 구현.

---

## 알아두면 좋은 점

- 캐릭터카드·월드인포·대화 히스토리는 ST가 알아서 컨텍스트에 넣어줌
- 페르소나 디스크립션 자동 반영
- 세션 히스토리는 새로고침하면 초기화됨 — 남기고 싶은 결과는 미리 복사해두기
- 프리셋·마지막 설정·연결 프로필 지정은 전부 영구 저장
- 문제 생기면 브라우저 개발자도구(F12) 콘솔에서 `[한마디]`로 시작하는 로그로 원인 확인 가능
